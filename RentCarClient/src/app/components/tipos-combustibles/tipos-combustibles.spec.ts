import { TiposCombustiblesComponent } from './tipos-combustibles';

describe('TiposCombustiblesComponent', () => {
  it('should export the component', () => {
    expect(TiposCombustiblesComponent).toBeTruthy();
  });
});
